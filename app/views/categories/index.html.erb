<ol class="breadcrumb">
<li><%= link_to "Strona Główna", :style =>'color:white;' %></li>
<li><a class="color:#428BCA;" href="#">Lista kategorii</a></li>
</ol> 

<%= link_to 'Dodaj nową kategorię', new_category_path, class: "btn btn-primary", :style =>'color:white;' %>
<table class="table table-striped">
  <thead>
    <tr>
    
      <th>Nazwa kategorii usługi</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @categories.each do |category| %>
      <tr>
     
        <td><%= category.name_category %></td>
        <td><%= link_to 'Edytuj', edit_category_path(category), class:"btn btn-success", :style =>'color:white;' %> <%= link_to 'Usuń', category, method: :delete, data: { confirm: 'Jesteś pewien?' },class:"btn btn-danger", :style =>'color:white;' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

